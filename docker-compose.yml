services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: rpg
      POSTGRES_PASSWORD: rpg_pass
      POSTGRES_DB: rpg_engine
    ports:
      - "5432:5432"
    volumes:
      - rpg_data:/var/lib/postgresql/data

volumes:
  rpg_data:
